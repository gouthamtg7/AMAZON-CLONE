// Import the cart array from data-products.js
import { cart } from './data-products.js'; // Ensure the path is correct based on your project structure

// Iterate over the cart array
cart.forEach(element => {
    console.log(element);
    // Perform operations with each cart item
    // Example: Displaying the items in the console or rendering them on a webpage
});
